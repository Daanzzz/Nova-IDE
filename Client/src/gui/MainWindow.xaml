<Window x:Class="Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Client"
        Title="Nova (v0.0.1)"
        Height="450"
        Width="800"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="#2e2d30">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Background="#2e2d30" Grid.Row="0" MouseDown="CustomTitleBar_MouseDown">
            <Grid>
                <Image Source="/img/NovaTransparent.png" Margin="5,5,0,0" HorizontalAlignment="Left"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button x:Name="MinimizeButton" Width="30" Height="30" Content="⏤" Click="MinimizeButton_Click" Background="#2e2d30" BorderThickness="0" FontWeight="Bold"/>
                    <Button x:Name="CloseButton" Width="30" Height="30" Content="╳" Click="CloseButton_Click" Background="#2e2d30" BorderThickness="0" FontWeight="Bold" FontStretch="Normal"/>
                    
                </StackPanel>
                
            </Grid>
        </Border>

        <ContentControl Name="MainContent" Grid.Row="1" />
    </Grid>
</Window>
